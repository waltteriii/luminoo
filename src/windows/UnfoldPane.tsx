import WindowFrame from './WindowFrame';

export default function UnfoldPane() {
  return (
    <WindowFrame title="Unfold" className="h-full">
      <div className="p-4 text-sm text-muted-foreground">
        Multi-time view (Offset Layer) — placeholder
      </div>
    </WindowFrame>
  );
}

